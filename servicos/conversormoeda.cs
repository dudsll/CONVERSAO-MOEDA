using System;
using System.Diagnostics.Contracts;

public class ConversorMoeda
{
    private ConversorMoeda conversorMoeda = new ConversorMoeda();

    public double ConverterMoeda(double valor, double cotacao))

        {
            if (opcao == 1 || opcao == 2)
                return valor / cotacao;
}